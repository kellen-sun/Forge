name: CI Test

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:
  test-apple-silicon:
    name: Test on M1 Mac
    runs-on: macos-14

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip' # Caches dependencies to speed up future runs

      - name: Install Build Dependencies
        run: |
          pip install --upgrade pip setuptools wheel cmake

      - name: Build Backend (CMake)
        run: |
          mkdir build
          cd build
          cmake ..
          cmake --build .

      - name: Install Library
        # Installs your library in editable mode
        run: pip install -e .

      - name: Install Test Dependencies
        run: pip install pytest torch torchvision torchaudio

      - name: Run Tests
        run: |
          pytest tests/
